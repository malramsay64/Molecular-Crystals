
# Determine system type
UNAME := $(shell uname)
HOST := $(shell hostname)

ifeq ($(HOST), gaucho.duckdns.org)
	SYS_NAME:=gaucho
else ifeq ($(HOST), ortho)
	SYS_NAME:=ortho
else ifeq ($(UNAME), Darwin)
    	SYS_NAME:=mac
else 
	SYS_NAME:=silica
endif
# vim:filetype=make
